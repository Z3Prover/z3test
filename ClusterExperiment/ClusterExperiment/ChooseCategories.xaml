<Window x:Class="ClusterExperiment.ChooseCategories"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choose Categories" Height="331" Width="225" WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
                
        <ListBox HorizontalAlignment="Stretch" Margin="0,0,0,0" Name="listBox" VerticalAlignment="Stretch" Grid.Row="0" Grid.Column="0" TabIndex="0" SelectionMode="Multiple" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1" Grid.Column="0" >
            <Button Name="okButton" IsDefault="True" Width="80" TabIndex="1" Click="okButton_Click">OK</Button>
            <Button Name="cancelButton" IsCancel="True" Width="80" TabIndex="2" Click="cancelButton_Click">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>
